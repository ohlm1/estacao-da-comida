/**
 * Ponto de compatibilidade retroativa para dados da landing page.
 *
 * A fonte única de verdade do projeto agora é `config/restaurant.config.ts`.
 * Todas as exportações e tipos são unificados a partir do arquivo central.
 */

export * from '@/config/restaurant.config'
